################### File Generated by Mangohud ###################

control=mangohud

# General settings
fsr_steam_sharpness=5  # Sharpness for FSR
nis_steam_sharpness=10  # Sharpness for NIS

legacy_layout=0  # Use the modern layout
horizontal  # Arrange information side by side

# GPU and system information
gpu_name=1  # Show GPU name at the top

# Battery settings
battery
battery_color=DDA0DD  # Light purple for the battery section

# GPU settings
gpu_stats
gpu_color=DDA0DD  # Light purple for GPU stats
gpu_temp=1  # Display GPU temperature
gpu_load=1  # Display GPU usage percentage

# CPU settings
cpu_stats
cpu_color=DDA0DD  # Light purple for CPU stats
cpu_temp=1  # Display CPU temperature
cpu_load=1  # Display CPU usage percentage
cpu_freq=1  # Display CPU frequency per core

# Power settings
cpu_power
gpu_power

# Memory settings
vram
vram_color=DDA0DD  # Light purple for VRAM usage
ram
ram_color=DDA0DD  # Light purple for RAM stats

# FPS settings
fps
frametime=0
frametime_color=DDA0DD  # Light purple for frametime
frame_timing=1  # Show detailed frame timing statistics

# HUD layout settings
#hud_no_margin  # Removes margins for a cleaner HUD
table_columns=14  # Ensures a single-line horizontal layout

# Additional options
io_stats  # Show disk I/O stats
io_color=DDA0DD  # Light purple for I/O stats
fps_limit=0  # Optionally limit the FPS (0 means no limit)
log_performance=1  # Log performance data to a file
